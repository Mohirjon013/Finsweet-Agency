import React, { useContext } from 'react'
import { Context } from '../Context/UserContext'

function  () {
    const {careerJob} = useContext(Context)
    
    return (
        <section>
            Carrer
        </section>
    )
}

export default  
